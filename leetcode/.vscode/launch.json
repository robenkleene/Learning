{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "lldb",
          "request": "launch",
          "name": "Debug",
          // Compile first with `cc -g <name>.c`
          "program": "${workspaceFolder}/a.out",
          "args": [],
          "cwd": "${workspaceFolder}"
      }
  ]
}